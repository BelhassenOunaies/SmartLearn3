{% extends 'basefront.html.twig' %}

{% block body %}
    <h1>Edit Course</h1>

    {{ include('courses_front/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('courses_front_index') }}">back to list</a>

    {{ include('courses_front/_delete_form.html.twig') }}
{% endblock %}
